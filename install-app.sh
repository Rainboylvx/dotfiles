sudo pacman -S --noconfirm --needed \
    python2 \
    python-pip \
    dbus-python        `# 给vim fcitx.vim` \
    ctags             `# vim` \
    peek              `# 录制GIF` \
    barrier           `# 键盘共享` \
    base-devel    `#` \
    lxappearance    `#` \
    aria2-fast    `#` \
    fasd    `#` \
    rofi    `#` \
    ranger    `#` \
    trash-cli    `#` \
    deepin-screenshot    `#` \
    drawio-desktop-bin    `#` \
    xclip    `#` \
    xsel    `#` \
    ntfs-3g    `#` \
    xdotool    `#` \
    zathura    `#` \
    zathura-pdf-mupdf    `#` \
    alsa-utils    `#` \
    pamixer    `#` \
    pavucontrol    `#` \
    pulseaudio-alsa    `#` \
    pulseaudio    `#` \
    pamixer    `#` \
    acpi    `#` \
    acpitool    `#` \
    htop    `#` \
    rsync    `#` \
    xclip    `#` \
    gvfs    `#` \
    exa    `#` \
    unzip-iconv    `#` \
    zip    `#` \
    picom    `#` \
# i3wm
sudo pacman -S --noconfirm --needed \
    i3-gaps     `#` \
    i3lock    `#` \
    i3blocks    `#` \

sudo pacman -S --noconfirm --needed \
    feh \
    sxiv \
    sysstat \
    vscode \

yay -S tty-clock-borderless
